when:
  - event: ["push", "pull_request"]
    branch: main

engine: nixery

dependencies:
  nixpkgs:
    - zig
    - rust

steps:
  - name: Run tests
    command: |
      zig build test --summary all

